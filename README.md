# CAM-Replication
CAM-Replication implements the Class Activation Mapping (CAM) technique from the original paper.  It allows a CNN trained on image-level labels to both classify images and localize discriminative regions in a single forward pass.
